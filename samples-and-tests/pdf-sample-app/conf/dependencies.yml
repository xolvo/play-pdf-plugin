# Application dependencies

require:
    - play
    - ru.purecode -> fopdf 0.4.0-beta

repositories:
    - purecode:
        type: local
        artifact: "${application.path}/../../fopdf/dist/[module]-[revision].zip"
        contains:
            - ru.purecode -> *
